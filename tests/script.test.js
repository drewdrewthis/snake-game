describe('Snake', function() {
	it('Progess at least every second', function() {

	});

	it('should get longer everytime it eats something', function() {
		
	});

	it('should get die when it hits a wall', function() {
		
	});

	it('should get die when it hits itself', function() {
		
	});

	it('should turn based on user commands', function() {
		
	});

	it('should turn based on user commands', function() {
		
	});

});